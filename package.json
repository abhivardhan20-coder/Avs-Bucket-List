{
  "name": "avs-bucket-list",
  "version": "1.0.0",
  "description": "AV's Bucket List - A comprehensive personal media tracker for your favorite movies and TV shows.",
  "author": "abhivardhan20-coder",
  "keywords": [
    "movie-tracker",
    "react",
    "express",
    "vite",
    "typescript",
    "drizzle",
    "monorepo"
  ],
  "homepage": "https://github.com/abhivardhan20-coder/Avs-Bucket-List",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "db:generate": "pnpm --filter @workspace/db run generate",
    "db:migrate": "pnpm --filter @workspace/db run migrate"
  },
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.61.1",
    "@typescript-eslint/parser": "^8.61.1",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.4",
    "typescript": "5.9.3"
  }
}
